请严格按以下步骤执行，完成后直接输出报告正文，不需要解释执行过程。

## 第一步：运行脚本获取基础数据

```python
import subprocess

r = subprocess.run(
    ["python3", "/workspace/skills/astock-report/scripts/send_close_summary.py", "--dry-run"],
    capture_output=True, text=True, timeout=120
)
base_output = r.stdout + r.stderr
print(base_output[-5000:] if len(base_output) > 5000 else base_output)
```

记录脚本输出内容，标记哪些字段为"暂缺"、"待核实"或为空。

## 第二步：缺失数据处理

- 价格、涨跌幅、成交量、成交额、北向资金、两融、主力资金、涨跌停、龙虎榜等数值字段，缺失时不得用 batch_web_search、新闻摘要或模型推断补齐。
- 龙虎榜净买入排行必须调用 `market_data_guard.py billboard --date YYYY-MM-DD --json`；AkShare `stock_lhb_detail_em` 为主源，且必须确认交易日期和核验状态。`status` 不是 `ok` 时不得输出排行。
- 直接调用原始接口时，先检查时间、字段、分页、单位、口径、去重和异常值；任一项无法确认就写"待核实"或"暂未获取"。
- 特别注意交易日/T-1延迟、代码歧义、复权口径、HTTP 200但业务为空、默认排序非时间、分页漏数、单位错位和摘要不是原文。
- 输出比例、排名、评分、归因或趋势判断前，先确认参与字段日期、单位、口径、缺失处理、排序方向和去重口径；缺失值不能按0处理，日期或口径不一致时写"暂不计算"。
- 涨跌停规则、新股/次新、ETF/LOF、可转债、跨市场证券、指数成分、除权除息、市值口径、估值极端值、缓存限流和旧记忆值均按高危异常处理。
- 可以搜索新闻标题、政策事件和公告链接，但只能作为「事件来源」，不能替代行情或资金数值。
- 缺失字段在报告中写"暂未获取"或"待核实"，并说明对应来源暂不可用。
- 当前价类问题必须改用 `market_data_guard.py quote <股票名或代码>` 实时取数。

## 第三步：生成收盘小结

报告保留以下结构；没有数据的项目写"暂未获取"，不要编数字。

```
📊 【A股收盘小结】YYYY年MM月DD日

━━━ 一，主要股指表现 ━━━
（列出可获取指数：指数名、点位、涨跌幅、来源和取数时间）

━━━ 二，资金流向 ━━━
• 北向资金：净流入/流出XX亿（来源+交易日期；若查不到写"暂未获取"）
• 主力资金：净流入/流出XX亿（来源+取数时间；若查不到写"暂未获取"）
• 两融余额：X.XX万亿（来源+交易日期；若查不到写"暂未获取"）

━━━ 三，板块表现 ━━━
✅ 涨幅前3：板块名 +X.XX%（来源+取数时间）
❌ 跌幅前3：板块名 -X.XX%（来源+取数时间）
（若查不到写"今日板块数据暂未获取"）

━━━ 四，涨跌停统计 ━━━
• 涨停家数：XX家（来源+交易日期）
• 跌停家数：XX家（来源+交易日期）
（若查不到写"暂未获取"）

━━━ 五，量化情绪打分 ━━━
缺任一关键字段时，综合评分写"数据暂缺，无法评分"，不要把缺失项按0分处理。

━━━ 数据来源：AKShare 主源 + 可追溯核验来源（按实际可用来源删减） ━━━
⚠️ 仅供参考，不构成投资建议。股市有风险，投资需谨慎。
```

## 第四步：推送

- 只有用户明确要求推送，且脚本不是 dry-run，才允许推送。
- 测试、排查、生成草稿时只输出报告正文，不调用 Webhook。
