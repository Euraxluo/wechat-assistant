# articles/ 已迁移

本目录已废弃。内容迁至：

| 原位置 | 新位置 |
|--------|--------|
| `articles/topic-plan-*.md` | `content/plans/` |
| `articles/article_*.html`（终稿） | `content/drafts/` |
| `articles/*预览*`、排版稿、旧版 | `content/archive/` |
| `articles/images/` | `content/assets/<slug>/` |
| `articles/*_data.json` | `data/snapshots/` |
| `covers/` | `content/assets/<slug>/` 或 `_legacy-covers/` |

发布时使用 `runs/<slug>.json` 指向 `content/` 下的路径。
